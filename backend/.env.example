# Copy this file to .env and fill values locally. Never commit .env.
APP_ENV=development
DEBUG=true

# Neon: use the asyncpg SQLAlchemy URL from the Neon Connect dialog.
# Replace the scheme `postgresql://` with `postgresql+asyncpg://`.
DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@HOST/DATABASE?sslmode=require
DEMO_DATA_MODE=false

# Enables the live Gemini declaration-extraction service.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite
MOCK_AI_MODE=false

# Generate a long unique value before any shared deployment.
JWT_SECRET=replace-with-a-long-random-secret
CORS_ORIGINS=http://localhost:*,http://127.0.0.1:*
STORAGE_ROOT=storage
